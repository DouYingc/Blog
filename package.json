{
  "name": "blog-system",
  "version": "1.0.0",
  "description": "Blog system project root",
  "scripts": {
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && npm run serve",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
